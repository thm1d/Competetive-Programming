#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(0);
    int n;
    cin>>n;
    int a[n-1],p[n];
    for(int i=0;i<n-1;i++)
        cin>>a[i];
    p[0]=1;
    for(int i=0;i<n-1;i++)
    {
        p[i+1]=q[i]+p[i]
    }
}
